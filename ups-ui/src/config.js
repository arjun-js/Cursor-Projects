/**
 * Default URL loaded in each pane on startup.
 * Add a port if the device does not use 80, e.g. http://192.168.163.160:8080
 */
export const DEFAULT_HOST = '192.168.163.160';

export const PANE_URLS = [
  `http://${DEFAULT_HOST}`,
  `http://${DEFAULT_HOST}`,
  `http://${DEFAULT_HOST}`,
  `http://${DEFAULT_HOST}`,
  `http://${DEFAULT_HOST}`,
];
